<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.collections.*?>
<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import java.lang.String?>

<BorderPane minHeight="700" minWidth="1024" styleClass="root"
            xmlns="http://javafx.com/javafx/17.0.12"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.sajid._207017_chashi_bhai.controllers.CropFeedController">

    <!-- Top Navigation -->
    <top>
        <VBox>
            <!-- Navigation Bar -->
            <HBox alignment="CENTER_LEFT" spacing="20" styleClass="nav-bar">
                <padding>
                    <Insets bottom="15" left="20" right="20" top="15"/>
                </padding>
                <Button fx:id="btnDashboard" onAction="#onDashboard" styleClass="button-primary" text="🏠 ড্যাশবোর্ড"/>
                <Label styleClass="page-title" text="সকল ফসল / Browse Crops"/>
                <Region HBox.hgrow="ALWAYS"/>
                <TextField fx:id="txtQuickSearch" prefWidth="250" promptText="খুঁজুন (নাম, জেলা, কৃষক, কোড)..." styleClass="quick-search"/>
                <Button fx:id="btnSearchUser" text="🔍 User ID" onAction="#onSearchUser" styleClass="button-secondary"/>
                <Button fx:id="btnProfile" onAction="#onProfile" styleClass="button-secondary" text="👤 প্রোফাইল"/>
            </HBox>

            <!-- Filter Bar -->
            <HBox alignment="CENTER_LEFT" spacing="15" styleClass="filter-bar">
                <padding>
                    <Insets bottom="12" left="20" right="20" top="12"/>
                </padding>
                <Label styleClass="filter-label" text="ফিল্টার:"/>

                <ComboBox fx:id="cbFilterCropType" prefWidth="180" promptText="ফসলের ধরন" styleClass="filter-combo">
                    <items>
                        <FXCollections fx:factory="observableArrayList">
                            <String fx:value="সব ধরন (All Types)"/>
                            <String fx:value="শস্য (Grains)"/>
                            <String fx:value="সবজি (Vegetables)"/>
                            <String fx:value="ফল (Fruits)"/>
                            <String fx:value="মসলা (Spices)"/>
                        </FXCollections>
                    </items>
                </ComboBox>

                <ComboBox fx:id="cbFilterDistrict" prefWidth="180" promptText="জেলা" styleClass="filter-combo">
                    <items>
                        <FXCollections fx:factory="observableArrayList">
                            <String fx:value="সব জেলা (All Districts)"/>
                            <String fx:value="ঢাকা (Dhaka)"/>
                            <String fx:value="চট্টগ্রাম (Chattogram)"/>
                            <String fx:value="রাজশাহী (Rajshahi)"/>
                            <String fx:value="খুলনা (Khulna)"/>
                        </FXCollections>
                    </items>
                </ComboBox>

                <Label styleClass="filter-label" text="মূল্য:"/>
                <Slider fx:id="sliderPriceMin" max="1000" min="0" prefWidth="120" styleClass="price-slider" value="0"/>
                <Label fx:id="lblPriceRange" styleClass="filter-label" text="৳ 0 - ৳ 1000"/>

                <CheckBox fx:id="chkVerifiedOnly" styleClass="filter-checkbox" text="✓ শুধু যাচাইকৃত"/>

                <Button fx:id="btnApplyFilter" onAction="#onApplyFilter" styleClass="button-primary" text="প্রয়োগ করুন"/>
                <Button fx:id="btnResetFilter" onAction="#onResetFilter" styleClass="button-secondary" text="রিসেট"/>
            </HBox>
        </VBox>
    </top>

    <!-- Center Feed -->
    <center>
        <ScrollPane fitToHeight="true" fitToWidth="true" styleClass="scroll-pane" vbarPolicy="ALWAYS">
            <content>
                <VBox spacing="20">
                    <padding>
                        <Insets bottom="20" left="30" right="30" top="20"/>
                    </padding>

                    <!-- Grid for Crop Cards -->
                    <GridPane fx:id="gridCropFeed" hgap="20" vgap="20">
                        <columnConstraints>
                            <ColumnConstraints minWidth="280" percentWidth="33.33"/>
                            <ColumnConstraints minWidth="280" percentWidth="33.33"/>
                            <ColumnConstraints minWidth="280" percentWidth="33.33"/>
                        </columnConstraints>
                    </GridPane>

                    <!-- Empty State -->
                    <VBox fx:id="vboxEmptyState" alignment="CENTER" spacing="20" visible="false">
                        <padding>
                            <Insets bottom="60" top="60"/>
                        </padding>
                        <Label styleClass="empty-icon" text="🔍"/>
                        <Label styleClass="empty-text" text="কোনো ফসল পাওয়া যায়নি"/>
                        <Label styleClass="empty-subtext" text="ফিল্টার পরিবর্তন করে আবার চেষ্টা করুন"/>
                    </VBox>
                </VBox>
            </content>
        </ScrollPane>
    </center>

</BorderPane>