<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.control.ScrollPane?>

<ScrollPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.sajid._207017_chashi_bhai.controllers.CreatePinController"
            fitToWidth="true"
            styleClass="scroll-pane">

<VBox alignment="TOP_CENTER" spacing="25.0" styleClass="root" minHeight="600" prefHeight="600">
    <padding>
        <Insets bottom="40.0" left="40.0" right="40.0" top="30.0"/>
    </padding>

    <!-- Back Button -->
    <HBox>
        <Button text="← Back" onAction="#onBackClick" styleClass="back-button">
        </Button>
    </HBox>

    <!-- PIN Form Container -->
    <VBox alignment="CENTER" spacing="20.0" 
          styleClass="card"
          maxWidth="500">
        <padding>
            <Insets bottom="40.0" left="40.0" right="40.0" top="40.0"/>
        </padding>

        <!-- Header -->
        <VBox alignment="CENTER" spacing="12.0">
            <Label text="🔑" styleClass="icon-small"/>
            <Label text="Create Your PIN" styleClass="label-title, title-large"/>
            <Label text="আপনার পিন তৈরি করুন" styleClass="label-subtitle, subtitle-bengali"/>
            <Label text="Secure your account with a strong PIN" styleClass="label-description, text-muted"/>
        </VBox>

        <!-- New PIN -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="🔑 New PIN (4-6 digits)" styleClass="field-label"/>
            <PasswordField fx:id="newPinField" promptText="Create a PIN"
                          styleClass="form-input"
                          prefWidth="420.0">
            </PasswordField>
        </VBox>

        <!-- Confirm PIN -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="✓ Confirm PIN" styleClass="field-label"/>
            <PasswordField fx:id="confirmPinField" promptText="Confirm PIN"
                          styleClass="form-input"
                          prefWidth="420.0">
            </PasswordField>
        </VBox>

        <!-- PIN Guidelines -->
        <VBox alignment="TOP_LEFT" spacing="6" styleClass="info-box">
            <Label text="📋 PIN Guidelines:" styleClass="field-label-small">
            </Label>
            <Label text="• Use 4-6 digits" styleClass="text-muted-tiny">
            </Label>
            <Label text="• Remember it well" styleClass="text-muted-tiny">
            </Label>
            <Label text="• Never share your PIN" styleClass="text-muted-tiny">
            </Label>
            <Label text="• Required for every login" styleClass="text-muted-tiny">
            </Label>
        </VBox>

        <!-- Error Label -->
        <Label fx:id="errorLabel" styleClass="error-text" visible="false" wrapText="true" maxWidth="400"/>

        <!-- Create Button -->
        <Button text="✓ Create PIN" onAction="#onCreatePinClick"
                styleClass="btn-continue"
                prefWidth="420.0" prefHeight="48.0"/>
    </VBox>
</VBox>
</ScrollPane>
