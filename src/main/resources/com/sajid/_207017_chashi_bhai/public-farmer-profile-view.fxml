<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.layout.FlowPane?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.shape.Circle?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="com.sajid._207017_chashi_bhai.controllers.PublicFarmerProfileController" styleClass="root" minWidth="1024">
    
    <!-- Top Navigation -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" styleClass="nav-bar">
            <padding>
                <Insets top="15" bottom="15" left="20" right="20"/>
            </padding>
            <Button text="←" styleClass="back-button" fx:id="btnBack" onAction="#onBack"/>
            <Label text="🌾 কৃষকের প্রোফাইল / Farmer Profile" styleClass="page-title"/>
            <Region HBox.hgrow="ALWAYS"/>
        </HBox>
    </top>

    <center>
        <ScrollPane fitToWidth="true" styleClass="scroll-pane">
            <VBox alignment="TOP_CENTER" spacing="30" styleClass="profile-container">
                <padding>
                    <Insets top="30" bottom="40" left="40" right="40"/>
                </padding>

                <!-- Profile Header Card -->
                <VBox alignment="CENTER" spacing="15" styleClass="card, profile-header">
                    <padding>
                        <Insets top="30" bottom="30" left="40" right="40"/>
                    </padding>
                    
                    <!-- Profile Photo -->
                    <StackPane alignment="CENTER">
                        <Circle radius="70" styleClass="profile-photo-border"/>
                        <ImageView fx:id="imgProfilePhoto" fitWidth="140" fitHeight="140" preserveRatio="false">
                            <clip>
                                <Circle radius="70" centerX="70" centerY="70"/>
                            </clip>
                        </ImageView>
                    </StackPane>

                    <!-- Name & Verified Badge -->
                    <VBox alignment="CENTER" spacing="8">
                        <Label fx:id="lblFarmerName" text="কৃষকের নাম" styleClass="profile-name"/>
                        <HBox alignment="CENTER" spacing="8">
                            <Label fx:id="lblVerifiedBadge" text="✓" styleClass="verified-badge" visible="false"/>
                            <Label text="যাচাইকৃত কৃষক" styleClass="verified-text" fx:id="lblVerifiedText" visible="false"/>
                        </HBox>
                    </VBox>

                    <!-- Contact Info -->
                    <VBox alignment="CENTER" spacing="8">
                        <HBox alignment="CENTER" spacing="8">
                            <Label text="🆔" styleClass="info-icon"/>
                            <Label fx:id="lblUserId" text="ID: 0000" styleClass="info-text"/>
                            <Button fx:id="btnCopyId" text="📋" styleClass="button-icon" onAction="#onCopyUserId" style="-fx-font-size: 12px; -fx-padding: 2 6;"/>
                        </HBox>
                        <HBox alignment="CENTER" spacing="8">
                            <Label text="📱" styleClass="info-icon"/>
                            <Label fx:id="lblPhone" text="+88017XXXXXXXX" styleClass="info-text"/>
                        </HBox>
                        <HBox alignment="CENTER" spacing="8">
                            <Label text="📍" styleClass="info-icon"/>
                            <Label fx:id="lblDistrict" text="জেলা" styleClass="info-text"/>
                        </HBox>
                    </VBox>
                </VBox>

                <!-- Stats Cards -->
                <FlowPane hgap="20" vgap="20" alignment="CENTER" maxWidth="920">
                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <Label fx:id="lblYearsFarming" text="5" styleClass="stat-value"/>
                        <Label text="বছর চাষাবাদ" styleClass="stat-label"/>
                    </VBox>

                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <Label fx:id="lblTotalProducts" text="0" styleClass="stat-value"/>
                        <Label text="মোট পণ্য" styleClass="stat-label"/>
                    </VBox>

                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <Label fx:id="lblTotalSales" text="0" styleClass="stat-value"/>
                        <Label text="মোট বিক্রয়" styleClass="stat-label"/>
                    </VBox>

                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <HBox alignment="CENTER" spacing="5">
                            <Label fx:id="lblRating" text="4.8" styleClass="stat-value"/>
                            <Label text="⭐" styleClass="stat-icon-small"/>
                        </HBox>
                        <Label text="রেটিং" styleClass="stat-label"/>
                        <Label text="Rating" styleClass="stat-sublabel"/>
                    </VBox>
                </FlowPane>

                <!-- Contact Actions -->
                <HBox fx:id="hboxContactActions" spacing="15" alignment="CENTER">
                    <Button fx:id="btnChat" text="💬 চ্যাট করুন" styleClass="button-primary" onAction="#onChat" prefWidth="200" prefHeight="45"/>
                    <Button fx:id="btnWhatsApp" text="📞 WhatsApp" styleClass="button-secondary" onAction="#onWhatsApp" prefWidth="200" prefHeight="45"/>
                </HBox>

                <!-- Active Products Section -->
                <VBox spacing="15" styleClass="section-container">
                    <Label text="🌾 সক্রিয় পণ্য / Active Products" styleClass="section-title"/>
                    <GridPane fx:id="gridProducts" hgap="20" vgap="20">
                        <!-- Products will be added dynamically -->
                    </GridPane>
                    <Label fx:id="lblNoProducts" text="কোনো সক্রিয় পণ্য নেই" styleClass="empty-text" visible="false"/>
                </VBox>

                <!-- Sales History Section -->
                <VBox spacing="15" styleClass="section-container">
                    <Label text="📊 বিক্রয় ইতিহাস / Sales History" styleClass="section-title"/>
                    <TableView fx:id="tblSalesHistory" styleClass="data-table" prefHeight="300">
                        <columns>
                            <TableColumn text="তারিখ / Date" prefWidth="120" fx:id="colDate"/>
                            <TableColumn text="পণ্য / Product" prefWidth="200" fx:id="colProduct"/>
                            <TableColumn text="পরিমাণ / Quantity" prefWidth="120" fx:id="colQuantity"/>
                            <TableColumn text="মূল্য / Price" prefWidth="120" fx:id="colPrice"/>
                            <TableColumn text="ক্রেতা / Buyer" prefWidth="180" fx:id="colBuyer"/>
                            <TableColumn text="স্ট্যাটাস / Status" prefWidth="120" fx:id="colStatus"/>
                        </columns>
                        <columnResizePolicy>
                            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
                        </columnResizePolicy>
                    </TableView>
                </VBox>

                <!-- Reviews Section -->
                <VBox spacing="15" styleClass="section-container">
                    <Label text="⭐ রিভিউ / Reviews" styleClass="section-title"/>
                    <VBox fx:id="vboxReviews" spacing="12">
                        <!-- Reviews will be added dynamically -->
                    </VBox>
                    <Label fx:id="lblNoReviews" text="কোনো রিভিউ নেই" styleClass="empty-text" visible="false"/>
                </VBox>

                <!-- Farm Photos Section -->
                <VBox spacing="15" styleClass="section-container">
                    <Label text="🖼️ খামারের ছবি / Farm Photos" styleClass="section-title"/>
                    <GridPane fx:id="gridFarmPhotos" hgap="15" vgap="15" alignment="CENTER">
                        <!-- Farm photos will be added dynamically -->
                    </GridPane>
                    <Label fx:id="lblNoFarmPhotos" text="কোনো খামারের ছবি নেই" styleClass="empty-text" visible="false"/>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>
    
</BorderPane>
