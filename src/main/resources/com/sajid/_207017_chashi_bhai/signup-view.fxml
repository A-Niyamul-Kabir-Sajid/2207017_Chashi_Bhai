<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.control.ScrollPane?>

<ScrollPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.sajid._207017_chashi_bhai.controllers.SignupController"
            fitToWidth="true"
            styleClass="scroll-pane">

<VBox alignment="TOP_CENTER" spacing="30.0" styleClass="root">
    <padding>
        <Insets bottom="40.0" left="40.0" right="40.0" top="30.0"/>
    </padding>

    <!-- Back Button -->
    <HBox>
        <Button text="← Back" onAction="#onBackClick" styleClass="back-button">
        </Button>
    </HBox>

    <!-- Signup Form Container -->
    <VBox alignment="CENTER" spacing="18.0" 
          styleClass="card"
          maxWidth="500">
        <padding>
            <Insets bottom="40.0" left="40.0" right="40.0" top="40.0"/>
        </padding>

        <!-- Header -->
        <VBox alignment="CENTER" spacing="12.0">
            <Label text="✍️" styleClass="icon-small"/>
            <Label text="Join Chashi Bhai" styleClass="label-title, title-large"/>
            <Label text="চাষী ভাইতে যোগ দিন" styleClass="label-subtitle, subtitle-bengali"/>
        </VBox>

        <!-- Name Input -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="👤 Full Name" styleClass="field-label"/>
            <TextField fx:id="nameField" promptText="Your full name"
                       styleClass="form-input"
                       prefWidth="420.0">
            </TextField>
        </VBox>

        <!-- Phone Input -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="📱 Phone Number" styleClass="field-label"/>
            <TextField fx:id="phoneField" promptText="01XXXXXXXXX"
                       styleClass="form-input"
                       prefWidth="420.0">
            </TextField>
        </VBox>

        <!-- District Selection -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="📍 District" styleClass="field-label"/>
            <ComboBox fx:id="districtCombo" promptText="Select your district"
                      styleClass="form-input"
                      prefWidth="420.0">
            </ComboBox>
        </VBox>

        <!-- Role Selection -->
        <VBox spacing="10.0">
            <Label text="🎯 I am a" styleClass="field-label"/>
            <HBox spacing="15" alignment="CENTER" prefWidth="420.0">
                <Button fx:id="farmerButton" text="👨‍🌾 Farmer" onAction="#onFarmerSelect"
                        styleClass="role-btn-farmer"
                        prefWidth="200" prefHeight="45"/>
                <Button fx:id="buyerButton" text="🛒 Buyer" onAction="#onBuyerSelect"
                        styleClass="role-btn-buyer"
                        prefWidth="200" prefHeight="45"/>
            </HBox>
        </VBox>

        <!-- PIN Input -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="🔐 Create PIN (4-6 digits)" styleClass="field-label"/>
            <PasswordField fx:id="pinField" promptText="Enter PIN"
                          styleClass="form-input"
                          prefWidth="420.0">
            </PasswordField>
        </VBox>

        <!-- Confirm PIN -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="✓ Confirm PIN" styleClass="field-label"/>
            <PasswordField fx:id="confirmPinField" promptText="Re-enter PIN"
                          styleClass="form-input"
                          prefWidth="420.0">
            </PasswordField>
        </VBox>

        <!-- Error Label -->
        <Label fx:id="errorLabel" styleClass="label-error" visible="false" wrapText="true" maxWidth="420"/>

        <!-- Continue Button -->
        <Button text="➜ Continue" onAction="#onContinueClick"
                styleClass="btn-continue"
                prefWidth="420.0" prefHeight="48.0"/>

        <!-- Login Link -->
        <HBox alignment="CENTER" spacing="8.0">
            <Label text="Already have an account?" styleClass="text-muted"/>
            <Button text="Login Here" onAction="#onLoginLinkClick"
                    styleClass="link-login"/>
        </HBox>
    </VBox>
</VBox>
</ScrollPane>
