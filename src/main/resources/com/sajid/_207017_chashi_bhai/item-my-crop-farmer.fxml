<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<HBox alignment="CENTER_LEFT" spacing="20" styleClass="crop-card" xmlns:fx="http://javafx.com/fxml/1" xmlns="http://javafx.com/javafx/25">
    <padding>
        <Insets bottom="15" left="20" right="20" top="15" />
    </padding>
    
    <!-- Crop Image -->
    <ImageView fx:id="cropImage" fitHeight="100" fitWidth="100" preserveRatio="true" styleClass="crop-image" />
    
    <!-- Crop Details -->
    <VBox spacing="8" HBox.hgrow="ALWAYS">
        <Label fx:id="lblCropName" styleClass="crop-name" text="ফসলের নাম" />
        <HBox spacing="15">
            <Label fx:id="lblPrice" styleClass="crop-price" text="৳ 0 / কেজি" />
            <Label styleClass="separator" text="•" />
            <Label fx:id="lblQuantity" styleClass="crop-quantity" text="0 কেজি উপলব্ধ" />
        </HBox>
        <HBox spacing="8">
            <Label styleClass="icon-small" text="📅" />
            <Label fx:id="lblDate" styleClass="crop-date" text="পোস্ট: --" />
        </HBox>
        <HBox spacing="8">
            <Label styleClass="icon-small" text="📍" />
            <Label fx:id="lblDistrict" styleClass="crop-district" text="জেলা: --" />
        </HBox>
        <Label fx:id="lblStatus" styleClass="status-active" text="✓ সক্রিয়" />
    </VBox>
    
    <!-- Action Buttons -->
    <VBox alignment="CENTER_RIGHT" spacing="10">
        <Button fx:id="btnEdit" prefWidth="120" styleClass="button-secondary" text="✎ সম্পাদনা" />
        <Button fx:id="btnDelete" prefWidth="120" styleClass="button-danger" text="🗑 মুছুন" />
    </VBox>
</HBox>
