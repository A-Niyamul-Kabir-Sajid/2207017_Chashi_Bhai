<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>

<HBox xmlns:fx="http://javafx.com/fxml" spacing="20" styleClass="crop-card" alignment="CENTER_LEFT">
    <padding>
        <Insets top="15" bottom="15" left="20" right="20"/>
    </padding>
    
    <!-- Crop Image -->
    <ImageView fx:id="cropImage" fitWidth="100" fitHeight="100" preserveRatio="true" styleClass="crop-image"/>
    
    <!-- Crop Details -->
    <VBox spacing="8" HBox.hgrow="ALWAYS">
        <Label fx:id="lblCropName" text="ফসলের নাম" styleClass="crop-name"/>
        <HBox spacing="15">
            <Label fx:id="lblPrice" text="৳ 0 / কেজি" styleClass="crop-price"/>
            <Label text="•" styleClass="separator"/>
            <Label fx:id="lblQuantity" text="0 কেজি উপলব্ধ" styleClass="crop-quantity"/>
        </HBox>
        <HBox spacing="8">
            <Label text="📅" styleClass="icon-small"/>
            <Label fx:id="lblDate" text="পোস্ট: --" styleClass="crop-date"/>
        </HBox>
        <HBox spacing="8">
            <Label text="📍" styleClass="icon-small"/>
            <Label fx:id="lblDistrict" text="জেলা: --" styleClass="crop-district"/>
        </HBox>
        <Label fx:id="lblStatus" text="✓ সক্রিয়" styleClass="status-active"/>
    </VBox>
    
    <!-- Action Buttons -->
    <VBox spacing="10" alignment="CENTER_RIGHT">
        <Button fx:id="btnEdit" text="✎ সম্পাদনা" styleClass="button-secondary" prefWidth="120"/>
        <Button fx:id="btnDelete" text="🗑 মুছুন" styleClass="button-danger" prefWidth="120"/>
        <Button fx:id="btnStats" text="📊 পরিসংখ্যান" styleClass="button-info" prefWidth="120"/>
    </VBox>
</HBox>
