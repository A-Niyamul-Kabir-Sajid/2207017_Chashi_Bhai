<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.shape.Circle?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="com.sajid._207017_chashi_bhai.controllers.BuyerProfileController" styleClass="root" minHeight="700" minWidth="1024">
    
    <!-- Top Navigation -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" styleClass="nav-bar">
            <padding>
                <Insets top="15" bottom="15" left="20" right="20"/>
            </padding>
            <Button text="←" styleClass="back-button" fx:id="btnBack" onAction="#onBack"/>
            <Label text="🌾 চাষী ভাই" styleClass="app-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button fx:id="btnEditProfile" text="✎ সম্পাদনা" styleClass="button-secondary" onAction="#onEditProfile"/>
        </HBox>
    </top>

    <center>
        <ScrollPane fitToWidth="true" styleClass="scroll-pane">
            <VBox alignment="TOP_CENTER" spacing="30" styleClass="profile-container">
                <padding>
                    <Insets top="30" bottom="40" left="40" right="40"/>
                </padding>

                <!-- Profile Header Card -->
                <VBox alignment="CENTER" spacing="15" styleClass="card, profile-header">
                    <padding>
                        <Insets top="30" bottom="30" left="40" right="40"/>
                    </padding>
                    
                    <!-- Profile Photo -->
                    <StackPane>
                        <Circle radius="70" styleClass="profile-photo-border"/>
                        <ImageView fx:id="imgProfilePhoto" fitWidth="130" fitHeight="130" preserveRatio="true">
                            <image>
                                <!-- Placeholder will be set in controller -->
                            </image>
                        </ImageView>
                    </StackPane>

                    <!-- Name & Title -->
                    <VBox alignment="CENTER" spacing="8">
                        <Label fx:id="lblBuyerName" text="ক্রেতার নাম" styleClass="profile-name"/>
                        <Label text="🛒 ক্রেতা / Buyer" styleClass="verified-text"/>
                    </VBox>

                    <!-- Contact Info -->
                    <VBox alignment="CENTER" spacing="8">
                        <HBox alignment="CENTER" spacing="8">
                            <Label text="📱" styleClass="info-icon"/>
                            <Label fx:id="lblPhone" text="+88018XXXXXXXX" styleClass="info-text"/>
                        </HBox>
                        <HBox alignment="CENTER" spacing="8" fx:id="hboxEmail" managed="false" visible="false">
                            <Label text="✉" styleClass="info-icon"/>
                            <Label fx:id="lblEmail" text="email@example.com" styleClass="info-text"/>
                        </HBox>
                        <HBox alignment="CENTER" spacing="8" fx:id="hboxAddress" managed="false" visible="false">
                            <Label text="📍" styleClass="info-icon"/>
                            <Label fx:id="lblAddress" text="ঠিকানা" styleClass="info-text"/>
                        </HBox>
                    </VBox>
                </VBox>

                <!-- Stats Cards -->
                <HBox spacing="20" alignment="CENTER">
                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <Label fx:id="lblTotalOrders" text="12" styleClass="stat-value"/>
                        <Label text="মোট অর্ডার" styleClass="stat-label"/>
                        <Label text="Total Orders" styleClass="stat-sublabel"/>
                    </VBox>

                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <Label fx:id="lblTotalSpent" text="৳ 25,000" styleClass="stat-value"/>
                        <Label text="মোট ব্যয়" styleClass="stat-label"/>
                        <Label text="Total Spent" styleClass="stat-sublabel"/>
                    </VBox>

                    <VBox alignment="CENTER" spacing="8" styleClass="stat-card">
                        <padding>
                            <Insets top="20" bottom="20" left="30" right="30"/>
                        </padding>
                        <Label fx:id="lblMemberSince" text="2024" styleClass="stat-value"/>
                        <Label text="সদস্য হয়েছেন" styleClass="stat-label"/>
                        <Label text="Member Since" styleClass="stat-sublabel"/>
                    </VBox>
                </HBox>

                <!-- Preferred Crops Section -->
                <VBox spacing="15" styleClass="section-container" fx:id="vboxPreferredCrops" managed="false" visible="false">
                    <Label text="পছন্দের ফসল / Preferred Crops" styleClass="section-title"/>
                    
                    <HBox spacing="10" fx:id="hboxPreferredCropsTags">
                        <!-- Preferred crop tags will be added dynamically -->
                        <Label text="🌾 ধান (Rice)" styleClass="crop-tag"/>
                        <Label text="🥔 আলু (Potato)" styleClass="crop-tag"/>
                        <Label text="🥕 সবজি (Vegetables)" styleClass="crop-tag"/>
                    </HBox>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>
    
</BorderPane>
