<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.control.ScrollPane?>

<ScrollPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.sajid._207017_chashi_bhai.controllers.ResetPinController"
            fitToWidth="true"
            styleClass="scroll-pane">

<VBox alignment="TOP_CENTER" spacing="25.0" styleClass="root" minHeight="700">
    <padding>
        <Insets bottom="40.0" left="40.0" right="40.0" top="30.0"/>
    </padding>

    <!-- Back Button -->
    <HBox>
        <Button text="← Back" onAction="#onBackClick" styleClass="back-button">
        </Button>
    </HBox>

    <!-- PIN Form Container -->
    <VBox alignment="CENTER" spacing="20.0" 
          styleClass="card"
          maxWidth="500">
        <padding>
            <Insets bottom="40.0" left="40.0" right="40.0" top="40.0"/>
        </padding>

        <!-- Header -->
        <VBox alignment="CENTER" spacing="12.0">
            <Label text="🔐" styleClass="icon-small"/>
            <Label text="Reset Your PIN" styleClass="label-title, title-large"/>
            <Label text="আপনার পিন রিসেট করুন" styleClass="label-subtitle, subtitle-bengali"/>
        </VBox>

        <!-- Phone Display -->
        <Label fx:id="phoneLabel" text="" styleClass="otp-phone-display"/>

        <!-- New PIN -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="🔑 New PIN (4-6 digits)" styleClass="field-label"/>
            <PasswordField fx:id="newPinField" promptText="Enter new PIN"
                          styleClass="form-input"
                          prefWidth="420.0">
            </PasswordField>
        </VBox>

        <!-- Confirm PIN -->
        <VBox spacing="8.0" prefWidth="420.0">
            <Label text="✓ Confirm PIN" styleClass="field-label"/>
            <PasswordField fx:id="confirmPinField" promptText="Confirm PIN"
                          styleClass="form-input"
                          prefWidth="420.0">
            </PasswordField>
        </VBox>

        <!-- PIN Requirements -->
        <VBox alignment="TOP_LEFT" spacing="6" styleClass="info-box">
            <Label text="📋 PIN Requirements:" styleClass="field-label-small">
            </Label>
            <Label text="• 4-6 digits only" styleClass="text-muted-tiny">
            </Label>
            <Label text="• Easy to remember" styleClass="text-muted-tiny">
            </Label>
            <Label text="• Don't share with anyone" styleClass="text-muted-tiny">
            </Label>
        </VBox>

        <!-- Error Label -->
        <Label fx:id="errorLabel" styleClass="error-text" visible="false" wrapText="true" maxWidth="400"/>

        <!-- Reset Button -->
        <Button text="🔄 Reset PIN" onAction="#onResetPinClick"
                styleClass="btn-reset"
                prefWidth="420.0" prefHeight="48.0"/>

        <!-- Back to Login -->
        <Button text="← Back to Login" onAction="#onBackToLoginClick"
                styleClass="link-back"/>
    </VBox>
</VBox>
</ScrollPane>
