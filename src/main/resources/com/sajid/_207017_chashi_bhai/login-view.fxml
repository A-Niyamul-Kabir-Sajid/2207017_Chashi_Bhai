<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.control.ScrollPane?>

<ScrollPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.sajid._207017_chashi_bhai.controllers.LoginController"
            fitToWidth="true"
            styleClass="scroll-pane">

<VBox alignment="TOP_CENTER" spacing="30.0" styleClass="root">
    <padding>
        <Insets bottom="40.0" left="40.0" right="40.0" top="30.0"/>
    </padding>

    <!-- Back Button -->
    <HBox>
        <Button text="← Back" onAction="#onBackClick" styleClass="back-button">
        </Button>
    </HBox>

    <!-- Login Form Container -->
    <VBox alignment="CENTER" spacing="20.0" 
          styleClass="card"
          maxWidth="480">
        <padding>
            <Insets bottom="40.0" left="40.0" right="40.0" top="40.0"/>
        </padding>

        <!-- Header -->
        <VBox alignment="CENTER" spacing="12.0">
            <Label text="🌾" styleClass="icon-small"/>
            <Label text="Welcome Back" styleClass="label-title, title-large"/>
            <Label text="লগইন করুন" styleClass="label-subtitle, subtitle-bengali"/>
        </VBox>

        <!-- Role Selection -->
        <VBox spacing="10.0" prefWidth="380.0">
            <Label text="👥 Login as / হিসাবে লগইন করুন" styleClass="field-label"/>
            <HBox spacing="15" alignment="CENTER">
                <Button fx:id="farmerButton" text="👨‍🌾 Farmer / কৃষক" onAction="#onFarmerSelect"
                        styleClass="role-button, role-btn-login"
                        prefWidth="180"/>
                <Button fx:id="buyerButton" text="🛍 Buyer / ক্রেতা" onAction="#onBuyerSelect"
                        styleClass="role-button, role-btn-login"
                        prefWidth="180"/>
            </HBox>
        </VBox>

        <!-- Phone Number Input -->
        <VBox spacing="8.0" prefWidth="380.0">
            <Label text="📱 Phone Number" styleClass="field-label"/>
            <TextField fx:id="phoneField" promptText="01XXXXXXXXX"
                       styleClass="text-field"
                       prefWidth="380.0"/>
        </VBox>

        <!-- PIN Input -->
        <VBox spacing="8.0" prefWidth="380.0">
            <Label text="🔐 PIN" styleClass="field-label"/>
            <PasswordField fx:id="pinField" promptText="Enter 4-6 digit PIN"
                          styleClass="password-field"
                          prefWidth="380.0">
            </PasswordField>
        </VBox>

        <!-- Error Label -->
        <Label fx:id="errorLabel" styleClass="label-error" visible="false" wrapText="true" maxWidth="380"/>

        <!-- Login Button -->
        <Button text="📱 Login / লগইন করুন" onAction="#onLoginClick"
                styleClass="button-primary"
                prefWidth="380.0" prefHeight="48.0">
            <font>
                <Font size="15.0"/>
            </font>
        </Button>

        <!-- Forgot Password Link -->
        <Button text="🔓 Forgot PIN? / পিন ভুলে গেছেন?" onAction="#onForgotPinClick"
                styleClass="button-link">
            <font>
                <Font size="12.0"/>
            </font>
        </Button>

        <!-- Divider -->
        <HBox alignment="CENTER" spacing="8.0">
            <Label text="Don't have an account?" styleClass="text-muted"/>
            <Button text="Sign Up" onAction="#onSignupLinkClick"
                    styleClass="link-signup"/>
        </HBox>
    </VBox>

    <!-- Footer -->
    <Label text="© 2025 Chashi Bhai - Secure Login" 
           styleClass="label-footer">
    </Label>
</VBox>
</ScrollPane>
