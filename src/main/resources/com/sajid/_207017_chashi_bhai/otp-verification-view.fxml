<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.control.ScrollPane?>

<ScrollPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.sajid._207017_chashi_bhai.controllers.OtpVerificationController"
            fitToWidth="true"
            styleClass="scroll-pane">

<VBox alignment="TOP_CENTER" spacing="25.0" styleClass="root">
    <padding>
        <Insets bottom="40.0" left="40.0" right="40.0" top="30.0"/>
    </padding>

    <!-- Back Button -->
    <HBox>
        <Button text="← Back" onAction="#onBackClick" styleClass="back-button">
        </Button>
    </HBox>

    <!-- OTP Form Container -->
    <VBox alignment="CENTER" spacing="20.0" 
          styleClass="card"
          maxWidth="500">
        <padding>
            <Insets bottom="40.0" left="40.0" right="40.0" top="40.0"/>
        </padding>

        <!-- Header -->
        <VBox alignment="CENTER" spacing="12.0">
            <Label text="📱" styleClass="icon-small"/>
            <Label text="Verify Phone" styleClass="otp-header"/>
            <Label text="ফোন যাচাই করুন" styleClass="otp-subtitle"/>
        </VBox>

        <!-- Phone Display -->
        <Label fx:id="phoneLabel" text="" styleClass="otp-phone-display"/>

        <!-- OTP Instructions -->
        <Label text="Enter the 6-digit code sent to your phone" styleClass="otp-instruction" wrapText="true" maxWidth="400"/>

        <!-- OTP Input Fields -->
        <HBox alignment="CENTER" spacing="10" maxWidth="400">
            <TextField fx:id="otp1" styleClass="otp-field"/>
            <TextField fx:id="otp2" styleClass="otp-field"/>
            <TextField fx:id="otp3" styleClass="otp-field"/>
            <TextField fx:id="otp4" styleClass="otp-field"/>
            <TextField fx:id="otp5" styleClass="otp-field"/>
            <TextField fx:id="otp6" styleClass="otp-field"/>
        </HBox>

        <!-- Timer -->
        <Label fx:id="timerLabel" text="00:60" styleClass="timer-label"/>

        <!-- Error Label -->
        <Label fx:id="errorLabel" styleClass="label-error" visible="false" wrapText="true" maxWidth="400"/>

        <!-- Verify Button -->
        <Button text="✓ Verify OTP" onAction="#onVerifyClick"
                styleClass="button-primary, otp-button"
                prefWidth="420.0" prefHeight="48.0"/>

        <!-- Resend OTP -->
        <Button fx:id="resendButton" text="📨 Resend OTP" onAction="#onResendOtpClick"
                styleClass="button-link, otp-resend-button"/>

        <!-- Debug OTP Section -->
        <VBox alignment="CENTER" spacing="5" styleClass="card-light" visible="true">
            <Label text="🔧 Development Mode" styleClass="debug-label"/>
            <Label fx:id="debugOtpLabel" text="OTP will appear here" styleClass="debug-label"/>
        </VBox>
    </VBox>
</VBox>
</ScrollPane>
